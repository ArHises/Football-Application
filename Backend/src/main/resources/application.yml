spring:
    application:
        name: roman
    datasource:
        url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/players}
        username: ${SPRING_DATASOURCE_USERNAME:root}
        password: ${SPRING_DATASOURCE_PASSWORD:FootBall123}
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        database-platform: org.hibernate.dialect.MySQL8Dialect
